import {Header,Footer,Banner} from '@/components/site-shell';import {services} from '@/lib/catalog';export default function Services(){return <><Header/><Banner eyebrow="The menu" title="Services made for you" description="Explore our treatments, then choose the specialist and time that suit you."/><main className="section wrap"><div className="service-grid">{services.map(s=><article className="service-card" key={s.id}><div className="service-visual"><img src={s.image} alt=""/></div><div className="service-body"><span className="eyebrow">{s.category}</span><h3 style={{marginTop:12}}>{s.name}</h3><p>{s.description}</p><div className="service-bottom"><span>${s.price} · {s.duration} min</span><a href={'/book?service='+s.id}>Book service →</a></div></div></article>)}</div></main><Footer/></>}
