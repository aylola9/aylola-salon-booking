import {Header,Footer,Banner} from '@/components/site-shell';import {stylists} from '@/lib/catalog';export default function Team(){return <><Header/><Banner eyebrow="Our people" title="Meet the team" description="Thoughtful professionals who make every appointment feel personal."/><main className="section wrap"><div className="team-grid">{stylists.map(s=><article className="team-card" key={s.id}><div className="team-monogram">{s.name[0]}</div><h2 className="serif">{s.name}</h2><span className="eyebrow">{s.role}</span><p className="intro">Specializing in {s.specialty.toLowerCase()}.</p><a className="btn btn-outline" href={'/book?service='+(s.id==='noor'?'facial':s.id==='lina'?'color':'signature-cut')+'&stylist='+s.id}>Book with {s.name} →</a></article>)}</div></main><Footer/></>}
